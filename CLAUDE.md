# CLAUDE.md — Legacy compatibility notice

The active desk is a **GPT/Codex desk**, not a Claude desk. Its authoritative operating rules are
in `AGENTS.md`; read that file, `MISSION.md`, and `docs/desk-cycle-runbook.md` before acting.

Non-negotiable summary: **PAPER ONLY**, `live=false`, no order-placement path, ChatGPT subscription
authentication, and `gpt-5.6-sol` at `xhigh` for the root and every inherited subagent. The agents
make decisions; deterministic code records them. The Adversary is the sole decision veto and a
rejected book gets at most one PM revision. Never substitute Claude/Opus agents for the required
GPT team.
